"use strict";

/*
Используя Math.random() необходимо сгенерировать массив, содержащий 5 цифр в 
диапазоне [0, 9].
После создания массива необходимо вывести в консоль следующие значения:
1. Сумму элементов массива
2. Минимальное значение в массиве
3. Новый массив, содержащий индексы сгенерированного выше массива, в которых 
значение равно 3.
Пример: Если у нас сгенерировался массив [2, 3, 5, 7, 3], то мы должны вывести 
в консоль массив [1, 4]. Такой массив получился потому что в сгенерированном
массиве тройки лежат под индексами 1 и 4. Если троек в сгенерированном массиве
не окажется, значит нужно будет вывести пустой массив.
*/

// Генерация массива из 5 цифр в диапазоне [0, 9]
const generatedArray = Array.from({ length: 5 }, () => Math.floor(Math.random() * 10));

// Вывод сгенерированного массива в консоль
console.log("Сгенерированный массив:", generatedArray);

// Вычисление суммы элементов массива
const sum = generatedArray.reduce((acc, val) => acc + val, 0);
console.log("Сумма элементов массива:", sum);

// Нахождение минимального значения в массиве
const min = Math.min(...generatedArray);
console.log("Минимальное значение в массиве:", min);

// Поиск индексов элементов, равных 3
const indicesOfThree = generatedArray.reduce((acc, val, index) => {
  if (val === 3) {
    acc.push(index);
  }
  return acc;
}, []);

// Вывод массива индексов, где значение равно 3
console.log("Индексы элементов со значением 3:", indicesOfThree);
